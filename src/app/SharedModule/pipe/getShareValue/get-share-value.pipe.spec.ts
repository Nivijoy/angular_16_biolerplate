import { GetShareValuePipe } from './get-share-value.pipe';

describe('GetShareValuePipe', () => {
  it('create an instance', () => {
    const pipe = new GetShareValuePipe();
    expect(pipe).toBeTruthy();
  });
});
