import { TypingEndsDirective } from './typing-ends.directive';

describe('TypingEndsDirective', () => {
  it('should create an instance', () => {
    const directive = new TypingEndsDirective();
    expect(directive).toBeTruthy();
  });
});
