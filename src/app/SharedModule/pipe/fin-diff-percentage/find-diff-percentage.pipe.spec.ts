import { FindDiffPercentagePipe } from './find-diff-percentage.pipe';

describe('FindDiffPercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new FindDiffPercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
