import { IsVisibleDirective } from './is-visible.directive';

describe('IsHiddenDirective', () => {
  it('should create an instance', () => {
    const directive = new IsVisibleDirective();
    expect(directive).toBeTruthy();
  });
});
