import { GetValueFromListPipe } from './get-value-from-list.pipe';

describe('GetValueFromListPipe', () => {
  it('create an instance', () => {
    const pipe = new GetValueFromListPipe();
    expect(pipe).toBeTruthy();
  });
});
