import { StyleCursorDirective } from './style-cursor.directive';

describe('StyleCursorDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleCursorDirective();
    expect(directive).toBeTruthy();
  });
});
