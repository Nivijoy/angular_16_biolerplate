import { GetTaxBasedPricePipe } from './get-tax-based-price.pipe';

describe('GetTaxBasedPricePipe', () => {
  it('create an instance', () => {
    const pipe = new GetTaxBasedPricePipe();
    expect(pipe).toBeTruthy();
  });
});
